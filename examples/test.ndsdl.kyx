ProgramVariables
  Real p;
  Real w;
  Real x;
  Real y;
  Real z;
End.

Problem
  [{p := 1;}{{{p := (0.5)*(p);}{{{p := (0.1)*(p);}{x := 1;}}++{{p := (0.9)*(p);}{x := 0;}}}}++{{{p := (0.5)*(p);}{{y := 1;}{{{p := (0.7)*(p);}{y := (y)+(1);}}*}}}++{{p := (0)*(p);}{{{p := (0.2)*(p);}{z := 0;}}++{{p := (0.8)*(p);}{z := (z)+(w);}}}}}}](true)
End.
